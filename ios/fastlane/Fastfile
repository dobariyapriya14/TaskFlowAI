default_platform(:ios)

platform :ios do
  desc "Build the iOS application"
  lane :build do
    build_app(
      workspace: "TaskFlowAI.xcworkspace",
      scheme: "TaskFlowAI",
      destination: "generic/platform=iOS Simulator",
      skip_archive: true
    )
  end
end
